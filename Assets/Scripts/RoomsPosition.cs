using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoomsPosition : MonoBehaviour
{
    public bool Occupied; // Variable que indica si la posición está libre (false) u ocupada (true)

    // Start is called before the first frame update
    void Start()
    {
        Occupied = false; // Por defecto establece que la posición está libre
    }

    // Update is called once per frame
    void Update()
    {

    }
}
